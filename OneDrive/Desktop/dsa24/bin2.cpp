#include <iostream>
using namespace std;

int bin(int arr[],int low,int high,int x){
    int mid=high+(high-low)/2;
    if(arr[mid]==x){
        return mid;
    }
    if(arr[mid]>x){
        bin(arr,)
    }
}